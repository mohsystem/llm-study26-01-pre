# Helps Gradle/JVM trust corporate MITM CAs installed in Windows certificate store.
# On Windows, this can fix SSLHandshakeException (PKIX path building failed) when downloading Gradle/Maven artifacts.
systemProp.javax.net.ssl.trustStoreType=Windows-ROOT

